<Page
    x:Class="Subsonic8.Views.ShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:micro="using:Caliburn.Micro"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <SplitView DisplayMode="Overlay" OpenPaneLength="120" IsPaneOpen="{Binding Path=NavigationPaneOpen, Mode=TwoWay}">
            <SplitView.Pane>
                <StackPanel Background="{ThemeResource SystemControlForegroundAccentBrush}">
                    <Button x:Name="ShowHome" Content="Device Specific Views" Margin="24" />
                </StackPanel>
            </SplitView.Pane>
            <SplitView.Content>
                <Grid>
                    <Frame micro:Message.Attach="[Event Loaded] = [SetupNavigationService($source)]" DataContext="{x:Null}" />
                    <Button x:Name="OpenNavigationPane" Content="&#xE700;" FontFamily="{ThemeResource SymbolThemeFontFamily}" VerticalAlignment="Top" Margin="24" />
                </Grid>
            </SplitView.Content>
        </SplitView>
    </Grid>
</Page>
